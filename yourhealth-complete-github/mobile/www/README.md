# YourHealth App Web Bundle

`index.html` is copied from:

```text
outputs/yourhealth/yourhealth-app-v9-patient-flow.html
```

When you update the app HTML, copy the new version here before running:

```bash
npx cap sync
```
